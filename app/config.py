import os

class Settings:
    #  path configuration, you can extend this based on your needs
    MODEL_PATH = 'app/model/best.pt'
    DATA_PATH = './data'
    # print(MODEL_PATH)

settings = Settings()
